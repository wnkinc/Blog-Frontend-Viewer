<div class="pad">
  <p class="pad-title">
    <span class="pad-line pad-line-1">
      I'm using AWS Cognito for authentication. The sign-in screen is their
      hosted UI with a custom background. Account creation requires email
      verification.
    </span>

    <span class="pad-line pad-line-2">
      But.. you can skip all that and just sign in here as RandomPanda!
    </span>
    <a href="<%= process.env.BLOG_API_BASE_URL %>/auth/guest"
      ><img src="/imgs/randompanda.png" alt="RandomPanda" class="pad-image"
    /></a>
    <span class="pad-line pad-line-3">
      We both know you weren’t going to create an account anyway!
    </span>
  </p>
  <a href="<%= process.env.BLOG_API_BASE_URL %>/auth/guest" class="pad-button"
    >Fine, I'll Sign In 😏</a
  >
</div>

<img class="balloon-box" src="/imgs/box.png" alt="Logo box" />
<div id="balloon-column"></div>
